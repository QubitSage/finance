/** Garante limites da mesada em contas que importaram batch-6 antes desta versão */
export const BATCH_8_ID = 'batch-8-mesada-limites'

export const BATCH_8_SETTINGS = {
  mesada: {
    limites: {
      estetica: 500,
      looks: 600,
      dates: 500,
      saida_livre: 400,
    },
  },
}
